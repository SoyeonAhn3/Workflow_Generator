# 테스트 시나리오 — Phase 2 앱 골격 네비게이션

- **생성일**: 2026-03-13
- **대상 Phase**: Phase 2
- **구현 기능**: TopNav (저장 뱃지, 사이드바 토글), Sidebar (부서/그룹/프로세스 행, 펼치기/접기, 통계, 부서추가), LV1View (그룹 카드), LV2View (프로세스 카드), LV3View (단계 상세), App.jsx 뷰 전환 로직
- **전제조건**: `npm run dev` 실행 후 `http://localhost:5173` 브라우저 접속
- **완료일**: 2026-03-16

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 | 결과 |
|----|------|------|------|---------|------|
| TC-001 | 정상 | 부서 클릭 → LV1 그룹 카드 표시 | 🤖 AI검증 | High | ✅ Pass |
| TC-002 | 정상 | LV1 그룹 카드 클릭 → LV2 프로세스 카드 표시 | 👤 수동 | High | ✅ Pass |
| TC-003 | 정상 | LV2 프로세스 카드 클릭 → LV3 단계 상세 표시 | 👤 수동 | High | ✅ Pass |
| TC-004 | 정상 | TopNav 저장됨 뱃지 정상 표시 | 🤖 AI검증 | High | ✅ Pass |
| TC-005 | 엣지 | 사이드바 접기/펼치기 transition 애니메이션 | 👤 수동 | High | ✅ Pass |
| TC-006 | 엣지 | 부서 클릭 시 사이드바 하위 그룹/프로세스 행 펼침 | 👤 수동 | High | ✅ Pass |
| TC-007 | 엣지 | LV3 보기 중 다른 부서 클릭 → LV1으로 리셋 | 👤 수동 | Medium | ✅ Pass |
| TC-008 | 실패 | 저장 실패 시 TopNav 뱃지 빨간색 전환 | 🤖 AI검증 | Medium | ✅ Pass |
| TC-009 | 실패 | 부서 미선택 상태 LV1 안내 메시지 표시 | 🤖 AI검증 | Medium | ✅ Pass |
| TC-010 | 실패 | 사이드바 닫힘 상태에서 통계·추가 버튼 숨김 | 🤖 AI검증 | Low | ✅ Pass |

---

### TC-001 [정상] 🤖 AI검증 — 부서 클릭 → LV1 그룹 카드 표시

**사전조건**: 앱 실행 중, 아무 것도 선택되지 않은 초기 상태
**실행 단계**:
1. 사이드바에서 "CO팀" 행 클릭
2. 메인 영역 확인

**입력값**: "CO팀" 부서 행 클릭
**예상 결과**: 메인 영역에 "마감 프로세스", "예산 관리 프로세스" 2개의 그룹 카드 표시. view 상태는 'lv1'
**확인 방법**: App.jsx의 `const view = selProc ? 'lv3' : selGroup ? 'lv2' : 'lv1'` 로직 및 LV1View 렌더 조건 코드로 확인 가능

**결과**: [x] Pass  [ ] Fail  **코멘트**: App.jsx:31 `const view = selProc ? 'lv3' : selGroup ? 'lv2' : 'lv1'` 로직 정상 구현. handleSelectDept에서 selGroup/selProc 초기화 확인.

---

### TC-002 [정상] 👤 수동 — LV1 그룹 카드 클릭 → LV2 프로세스 카드 표시

**사전조건**: TC-001 완료 후 (LV1에 그룹 카드가 표시된 상태)
**실행 단계**:
1. "마감 프로세스" 그룹 카드 클릭
2. 메인 영역 확인

**입력값**: "마감 프로세스" 카드 클릭
**예상 결과**: 메인 영역이 LV2로 전환되며 "물류마감 Check 프로세스", "가공비 마감", "재료비 마감" 3개의 프로세스 카드 표시
**확인 방법**: 카드 내 "담당:", "모듈:", "단계: N개" 정보가 보이면 LV2 정상 렌더링

**결과**: [x] Pass  [ ] Fail  **코멘트**: 사용자 수동 확인 완료.

---

### TC-003 [정상] 👤 수동 — LV2 프로세스 카드 클릭 → LV3 단계 상세 표시

**사전조건**: TC-002 완료 후 (LV2에 프로세스 카드가 표시된 상태)
**실행 단계**:
1. "물류마감 Check 프로세스" 카드 클릭
2. 메인 영역 확인

**입력값**: "물류마감 Check 프로세스" 카드 클릭
**예상 결과**: LV3 상세 화면으로 전환, "1. 물류 미결 현황 조회", "2. 미결 항목 담당부서 통보", "3. 처리 완료 확인" 3개의 단계 카드 표시. 각 카드에 화면명(T-Code/프로그램), PT, 담당 표시
**확인 방법**: "화면명 (T-Code/프로그램): MB52" 텍스트가 보이면 정상

**결과**: [x] Pass  [ ] Fail  **코멘트**: 사용자 수동 확인 완료.

---

### TC-004 [정상] 🤖 AI검증 — TopNav 저장됨 뱃지 정상 표시

**사전조건**: 앱 실행 중
**실행 단계**:
1. 화면 우측 상단 뱃지 확인

**입력값**: 없음 (자동 표시)
**예상 결과**: 녹색 배경의 "💾 저장됨 · NKB" 뱃지 표시 (N은 실제 KB 수치)
**확인 방법**: TopNav.jsx 코드에서 `storageOk ? '💾 저장됨...' : '⚠️ 저장 실패'` 조건부 렌더링 확인 가능

**결과**: [x] Pass  [ ] Fail  **코멘트**: TopNav.jsx:49 `background: storageOk ? '#16a34a' : C.red` 및 조건부 텍스트 렌더링 정상 구현 확인.

---

### TC-005 [엣지] 👤 수동 — 사이드바 접기/펼치기 transition 애니메이션

**사전조건**: 앱 실행 중, 사이드바 펼쳐진 상태(너비 250px)
**실행 단계**:
1. TopNav 좌측 "☰" 버튼 클릭
2. 사이드바 너비 변화 관찰
3. 다시 "☰" 버튼 클릭

**입력값**: ☰ 버튼 클릭 (2회)
**예상 결과**: 1회 클릭 시 사이드바가 48px로 부드럽게 좁아지며 텍스트 사라짐 (0.25s 애니메이션). 2회 클릭 시 250px로 복원되며 텍스트 표시
**확인 방법**: 사이드바가 즉시 사라지지 않고 슬라이드 애니메이션 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 사용자 수동 확인 완료.

---

### TC-006 [엣지] 👤 수동 — 부서 클릭 시 사이드바 하위 행 펼침

**사전조건**: 앱 실행 중, 사이드바 펼쳐진 상태
**실행 단계**:
1. 사이드바에서 "CO팀" 행 클릭
2. 사이드바 내부에서 그룹 행 표시 여부 확인
3. 그룹 행("마감 프로세스") 클릭
4. 하위 프로세스 행 표시 여부 확인

**입력값**: "CO팀" → "마감 프로세스" 순서로 클릭
**예상 결과**: CO팀 클릭 시 사이드바에 "마감 프로세스", "예산 관리 프로세스" 그룹 행 표시. 그룹 클릭 시 하위 프로세스 행 표시
**확인 방법**: 사이드바 내부에서 들여쓰기된 그룹명, 그 아래 ● 기호와 함께 프로세스명 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 사용자 수동 확인 완료.

---

### TC-007 [엣지] 👤 수동 — LV3 보기 중 다른 부서 클릭 → LV1으로 리셋

**사전조건**: TC-003 완료 후 (LV3 단계 상세 화면 표시 중)
**실행 단계**:
1. 사이드바에서 "재무팀" 클릭
2. 메인 영역 확인

**입력값**: LV3 상태에서 "재무팀" 클릭
**예상 결과**: LV1으로 전환되며 재무팀의 "월 마감 프로세스" 그룹 카드 1개 표시. 이전 LV3 내용 사라짐
**확인 방법**: 화면 상단에 "재무팀" 텍스트 표시, "그룹 1개" 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 사용자 수동 확인 완료.

---

### TC-008 [실패] 🤖 AI검증 — 저장 실패 시 TopNav 뱃지 빨간색 전환

**사전조건**: 앱 실행 중
**실행 단계**:
1. DevTools Console에서 아래 코드로 saveToStorage 반환값 확인:
```js
import('/src/storage.js').then(({ saveToStorage }) => {
  const result = saveToStorage([])
  console.log('결과:', result)
})
```

**입력값**: 빈 배열 저장 (정상 케이스로 result.ok 확인)
**예상 결과**: `{ ok: true, sizeKB: ... }` 반환. TopNav에서 `storageOk = result?.ok !== false`로 평가되어 ok=false일 때 빨간 뱃지 표시
**확인 방법**: TopNav.jsx 코드에서 `storageOk` 조건 및 빨간색(`C.red`) 배경 적용 코드 확인 가능

**결과**: [x] Pass  [ ] Fail  **코멘트**: storage.js:15 `QuotaExceededError` 시 `{ ok: false }` 반환 로직 확인. App.jsx:57 `storageOk = storageResult?.ok !== false` 조건 정상 구현.

---

### TC-009 [실패] 🤖 AI검증 — 부서 미선택 상태 LV1 안내 메시지 표시

**사전조건**: 앱 최초 실행 직후 (아무 것도 선택되지 않은 상태)
**실행 단계**:
1. 앱 초기 화면 메인 영역 확인 (사이드바에서 아무것도 선택하지 않음)

**입력값**: 없음
**예상 결과**: 메인 영역 중앙에 "사이드바에서 부서를 선택하세요" 안내 텍스트 표시
**확인 방법**: LV1View.jsx 코드에서 `if (!dept) return <안내 메시지>` 조건부 렌더링 확인 가능

**결과**: [x] Pass  [ ] Fail  **코멘트**: LV1View.jsx:8 `if (!dept) return (...)` 조건 및 "사이드바에서 부서를 선택하세요" 텍스트 정상 구현 확인.

---

### TC-010 [실패] 🤖 AI검증 — 사이드바 닫힘 상태에서 통계·추가 버튼 숨김

**사전조건**: 앱 실행 중, 사이드바 펼쳐진 상태
**실행 단계**:
1. TopNav "☰" 버튼 클릭 (사이드바 닫기)
2. 사이드바 하단 통계 위젯 및 "부서 추가" 버튼 영역 확인

**입력값**: 사이드바 닫기
**예상 결과**: 사이드바 닫힘 상태(48px)에서 통계 위젯과 "부서 추가" 버튼이 보이지 않음. 각 부서 아이콘만 표시
**확인 방법**: Sidebar.jsx 코드에서 `{open && (하단 위젯)}` 조건부 렌더링 확인 가능

**결과**: [x] Pass  [ ] Fail  **코멘트**: Sidebar.jsx:133 `{open && (<div>통계+부서추가버튼</div>)}` 조건부 렌더링 정상 구현 확인.

---

## 테스트 완료 체크리스트

- [x] 모든 정상 케이스 Pass (TC-001 ~ TC-004)
- [x] 사이드바 행 클릭으로 뷰 전환 정상 동작 (TC-002, TC-003)
- [x] 사이드바 펼치기/접기 상태 전환 정상 (TC-005, TC-006, TC-010)
- [x] 다른 부서 선택 시 뷰 상태 리셋 확인 (TC-007)
- [x] 저장 상태 뱃지 조건부 표시 확인 (TC-004, TC-008)

# 테스트 시나리오 — Phase 1 저장소 레이어

- **생성일**: 2026-03-13
- **대상 Phase**: Phase 1
- **구현 기능**: storage.js (LocalStorage 저장/불러오기), imageDB.js (IndexedDB 이미지 CRUD), App.jsx updateData 자동 저장 패턴
- **전제조건**: `npm run dev` 실행 후 `http://localhost:5173` 브라우저 접속, 개발자 도구(F12) 열어둠

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | 최초 실행 시 SAMPLE_DATA 자동 로드 | 🤖 AI검증 | High |
| TC-002 | 정상 | 부서 클릭 시 LocalStorage 자동 저장 확인 | 🤖 AI검증 | High |
| TC-003 | 정상 | 새로고침 후 LocalStorage에서 데이터 복원 | 👤 수동 | High |
| TC-004 | 정상 | IndexedDB 이미지 저장 및 불러오기 | 👤 수동 | High |
| TC-005 | 엣지 | deleteImages([]) 빈 배열 호출 시 에러 없음 | 🤖 AI검증 | Medium |
| TC-006 | 엣지 | 존재하지 않는 이미지 id 조회 | 👤 수동 | Medium |
| TC-007 | 엣지 | LocalStorage 데이터 손상 시 SAMPLE_DATA 폴백 | 🤖 AI검증 | Medium |
| TC-008 | 실패 | 브라우저 콘솔에서 저장 크기 경고 확인 | 🤖 AI검증 | Low |
| TC-009 | 실패 | 존재하지 않는 id로 deleteImage 호출 | 👤 수동 | Medium |
| TC-010 | 실패 | LocalStorage 강제 초기화 후 재기동 | 👤 수동 | High |

---

### TC-001 [정상] 🤖 AI검증 — 최초 실행 시 SAMPLE_DATA 자동 로드

**사전조건**: LocalStorage에 `processflow_v1` 키가 없는 상태 (DevTools → Application → Local Storage → 해당 키 삭제)
**실행 단계**:
1. 브라우저에서 `http://localhost:5173` 접속
2. 화면 중앙 디버그 패널 확인
3. 부서 수 숫자 확인

**입력값**: 없음 (자동 로드)
**예상 결과**: 디버그 패널에 "부서 수: 2" 표시, 하단 버튼에 "CO팀", "재무팀" 두 개 표시됨
**확인 방법**: DevTools → Console에서 `JSON.parse(localStorage.getItem('processflow_v1'))` 실행 후 배열 길이 2 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-002 [정상] 🤖 AI검증 — 부서 클릭 시 LocalStorage 자동 저장 확인

**사전조건**: 앱이 실행 중이고 디버그 패널이 보이는 상태
**실행 단계**:
1. 화면 하단 "CO팀" 버튼 클릭
2. 디버그 패널 "LocalStorage 저장 크기" 수치 확인
3. 상단 바의 "💾 저장됨 · NKB" 뱃지 확인

**입력값**: "CO팀" 버튼 클릭
**예상 결과**: 상단 뱃지가 녹색으로 "💾 저장됨 · NKB" (N은 실제 KB 수치) 표시, 디버그 패널 "선택된 부서: CO팀" 으로 변경
**확인 방법**: DevTools → Application → Local Storage → `processflow_v1` 키 존재 및 JSON 데이터 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-003 [정상] 👤 수동 — 새로고침 후 LocalStorage에서 데이터 복원

**사전조건**: TC-002 완료 후 (LocalStorage에 데이터 저장된 상태)
**실행 단계**:
1. 브라우저 새로고침 (F5)
2. 디버그 패널 확인
3. 상단 뱃지 확인

**입력값**: 없음 (새로고침)
**예상 결과**: 새로고침 후에도 "부서 수: 2" 유지, 상단 뱃지 "💾 저장됨 · NKB" 표시 (SAMPLE_DATA로 초기화되지 않음)
**확인 방법**: DevTools → Console에서 `localStorage.getItem('processflow_v1')` 실행 시 null이 아닌 JSON 문자열 반환 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-004 [정상] 👤 수동 — IndexedDB 이미지 저장 및 불러오기

**사전조건**: 앱 실행 중, DevTools Console 열린 상태
**실행 단계**:
1. DevTools → Console에서 아래 코드 실행:
```js
import('/src/imageDB.js').then(async ({ saveImage, loadImage }) => {
  const blob = new Blob(['test'], { type: 'image/png' })
  await saveImage('img_test001', blob, 'test.png')
  const result = await loadImage('img_test001')
  console.log('저장된 이미지:', result)
})
```
2. Console 출력 확인

**입력값**: id: `img_test001`, blob: 테스트 Blob, name: `test.png`
**예상 결과**: Console에 `{ id: 'img_test001', blob: Blob, name: 'test.png', createdAt: [숫자] }` 객체 출력
**확인 방법**: DevTools → Application → IndexedDB → `processflow_images` → `images` 스토어에 `img_test001` 레코드 존재 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-005 [엣지] 🤖 AI검증 — deleteImages([]) 빈 배열 호출 시 에러 없음

**사전조건**: 앱 실행 중, DevTools Console 열린 상태
**실행 단계**:
1. DevTools → Console에서 아래 코드 실행:
```js
import('/src/imageDB.js').then(async ({ deleteImages }) => {
  await deleteImages([])
  console.log('빈 배열 삭제 완료 — 에러 없음')
})
```
2. Console 출력 확인

**입력값**: `deleteImages([])`
**예상 결과**: Console에 "빈 배열 삭제 완료 — 에러 없음" 출력, 에러 메시지 없음
**확인 방법**: Console에 빨간 에러 텍스트 없음 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-006 [엣지] 👤 수동 — 존재하지 않는 이미지 id 조회

**사전조건**: 앱 실행 중, DevTools Console 열린 상태
**실행 단계**:
1. DevTools → Console에서 아래 코드 실행:
```js
import('/src/imageDB.js').then(async ({ loadImage }) => {
  const result = await loadImage('img_없는id_9999')
  console.log('결과:', result)
})
```
2. Console 출력 확인

**입력값**: 존재하지 않는 id `img_없는id_9999`
**예상 결과**: Console에 `결과: undefined` 출력 (에러 발생하지 않음)
**확인 방법**: Console에 빨간 에러 없이 `undefined` 출력 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-007 [엣지] 🤖 AI검증 — LocalStorage 데이터 손상 시 SAMPLE_DATA 폴백

**사전조건**: 앱 실행 중, DevTools Console 열린 상태
**실행 단계**:
1. DevTools → Console에서 아래 코드 실행:
```js
localStorage.setItem('processflow_v1', '{ 잘못된 JSON !!!')
```
2. 브라우저 새로고침 (F5)
3. 디버그 패널 확인

**입력값**: 손상된 JSON 문자열
**예상 결과**: 새로고침 후 앱이 크래시 없이 로드되고, "부서 수: 2" (SAMPLE_DATA) 표시
**확인 방법**: 화면에 에러 화면 없이 정상 UI 로드, 디버그 패널 "부서 수: 2" 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-008 [실패] 🤖 AI검증 — 브라우저 콘솔 저장 크기 경고 임계값 확인

**사전조건**: 앱 실행 중, DevTools Console 열린 상태
**실행 단계**:
1. DevTools → Console에서 아래 코드 실행 (3MB 초과 데이터 강제 저장):
```js
import('/src/storage.js').then(({ saveToStorage }) => {
  const bigData = [{ id: 'd1', name: 'test', icon: 'T', color: '#000',
    groups: [{ id: 'g1', name: 'g', processes: Array(500).fill(null).map((_, i) => ({
      id: `p${i}`, name: 'x'.repeat(1000), dept: 'CO팀', owner: '', module: 'CO',
      updatedAt: '', description: '', steps: []
    }))}]
  }]
  const result = saveToStorage(bigData)
  console.log('저장 결과:', result)
})
```
2. Console 출력 확인

**입력값**: 3MB 초과 대용량 데이터
**예상 결과**: Console에 `Storage size warning: NNNN KB` 경고 메시지 출력
**확인 방법**: Console에 노란 경고(warn) 텍스트로 "Storage size warning" 포함 메시지 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-009 [실패] 👤 수동 — 존재하지 않는 id로 deleteImage 호출

**사전조건**: 앱 실행 중, DevTools Console 열린 상태
**실행 단계**:
1. DevTools → Console에서 아래 코드 실행:
```js
import('/src/imageDB.js').then(async ({ deleteImage }) => {
  await deleteImage('img_없는id_9999')
  console.log('삭제 완료 — 에러 없음')
})
```
2. Console 출력 확인

**입력값**: 존재하지 않는 id `img_없는id_9999`
**예상 결과**: Console에 "삭제 완료 — 에러 없음" 출력 (IndexedDB는 없는 키 삭제 시 에러 발생 안 함)
**확인 방법**: Console에 빨간 에러 없음 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-010 [실패] 👤 수동 — LocalStorage 강제 초기화 후 재기동

**사전조건**: 앱 실행 중, LocalStorage에 데이터 저장된 상태
**실행 단계**:
1. DevTools → Application → Local Storage → `processflow_v1` 키 우클릭 → Delete
2. 브라우저 새로고침 (F5)
3. 디버그 패널 확인

**입력값**: LocalStorage 완전 삭제
**예상 결과**: 새로고침 후 SAMPLE_DATA로 초기화되어 "부서 수: 2" 표시, 상단 뱃지 정상 표시
**확인 방법**: 화면에 에러 없이 CO팀/재무팀 버튼 2개 표시, DevTools Console 에러 없음 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

## 테스트 완료 체크리스트

- [ ] 모든 정상 케이스 Pass (TC-001 ~ TC-004)
- [ ] 엣지 케이스에서 앱이 크래시 없이 처리됨 (TC-005 ~ TC-007)
- [ ] 실패 케이스에서 에러가 사용자에게 노출되지 않고 안전하게 처리됨 (TC-008 ~ TC-010)
- [ ] 새로고침 후에도 데이터가 LocalStorage에서 정상 복원됨
- [ ] IndexedDB `processflow_images` 스토어 정상 생성 확인

# 테스트 시나리오 — 병렬 분기·합류 다이어그램

- **생성일**: 2026-03-19
- **대상 Phase**: Phase 8
- **구현 기능**: SwimLane 다이어그램에 분기(Fork)·합류(Join) 흐름 추가 — colIndex 기반 열 배치 + SVG 오버레이 화살표 + connections 자동 파생 + StepModal Step 필드 + LV3 colIndex 정렬
- **전제조건**: 앱 실행 중 (`npm run dev`), localStorage 초기화 후 SAMPLE_DATA 로드 완료

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | 병렬 스텝이 같은 열에 배치됨 | 👤 수동 | High |
| TC-002 | 정상 | 분기 화살표 2개 렌더링 | 👤 수동 | High |
| TC-003 | 정상 | 합류 화살표 렌더링 | 👤 수동 | High |
| TC-004 | 정상 | 기존 선형 프로세스 하위 호환 | 👤 수동 | High |
| TC-005 | 엣지 | colIndex 없는 스텝 마지막 열 자동 배정 | 🤖 AI검증 | Medium |
| TC-006 | 엣지 | connections 자동 파생 (colIndex 그룹핑) | 🤖 AI검증 | Medium |
| TC-007 | 엣지 | 같은 colIndex 스텝이 여러 레인에 정상 배치 | 👤 수동 | Medium |
| TC-008 | 실패 | 존재하지 않는 stepId의 DOM ref null 가드 | 🤖 AI검증 | High |
| TC-009 | 실패 | 빈 steps 배열 처리 (hooks 순서 규칙 준수) | 🤖 AI검증 | Medium |
| TC-010 | 실패 | 단일 스텝만 존재 시 화살표 없이 렌더링 | 👤 수동 | Low |
| TC-011 | 정상 | StepModal Step 필드에서 colIndex 저장 | 👤 수동 | High |
| TC-012 | 정상 | LV3 단계 목록이 colIndex 기준 정렬 | 👤 수동 | High |
| TC-013 | 엣지 | 같은 부서+같은 colIndex 스텝이 셀 안에 모두 표시 | 🤖 AI검증 | Medium |
| TC-014 | 정상 | 번호 뱃지가 colIndex 정렬 순서 반영 | 🤖 AI검증 | High |

---

### TC-001 [정상] 👤 수동 — 병렬 스텝이 같은 열에 배치됨

**사전조건**: 앱 실행 중, CO팀 > 병렬 흐름 테스트 그룹 선택, `부서 병렬 처리 프로세스` 카드 ▼ 펼침

**실행 단계**:
1. 사이드바에서 `CO팀 > 병렬 흐름 테스트` 클릭
2. `부서 병렬 처리 프로세스` 카드의 ▼ 버튼 클릭 (부서별 업무 흐름 확장)
3. SwimLane 다이어그램 확인

**입력값**: 없음 (더미 데이터 사용)

**예상 결과**:
- 그리드가 3열로 구성됨 (열0: CO팀 Step1 / 열1: FIN팀+LOG팀 / 열2: CO팀 Step4)
- FIN팀 레인의 `재무 데이터 검토`와 LOG팀 레인의 `물류 데이터 검토`가 **같은 열**에 위치
- CO팀 레인에는 열1이 빈 셀로 표시됨

**확인 방법**: 다이어그램 2번째 열(FIN팀 박스와 LOG팀 박스)이 세로로 나란히 정렬돼 있는지 시각적으로 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-002 [정상] 👤 수동 — 분기 화살표 2개 렌더링

**사전조건**: TC-001과 동일, SwimLane 다이어그램 확인 가능 상태

**실행 단계**:
1. SwimLane 다이어그램에서 CO팀 `미결 현황 파악` 박스(Step1) 오른쪽 영역 확인
2. 화살표가 FIN팀 방향, LOG팀 방향으로 각각 뻗어 있는지 확인

**입력값**: 없음

**예상 결과**:
- `미결 현황 파악` 노드에서 **화살표 2개** 출발
- 하나는 FIN팀 레인의 `재무 데이터 검토`로 연결 (꺾인 선)
- 하나는 LOG팀 레인의 `물류 데이터 검토`로 연결 (꺾인 선)
- 레인이 다르므로 두 화살표 모두 **회색 점선**으로 표시

**확인 방법**: SVG 오버레이 선이 서로 다른 y 좌표의 두 노드를 향해 ㄱ자로 꺾여 연결되는지 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-003 [정상] 👤 수동 — 합류 화살표 렌더링

**사전조건**: TC-001과 동일, SwimLane 다이어그램 확인 가능 상태

**실행 단계**:
1. SwimLane 다이어그램에서 열2의 CO팀 `결과 종합 및 마감` 박스(Step4) 왼쪽 영역 확인
2. FIN팀과 LOG팀 박스에서 Step4로 향하는 화살표 확인

**입력값**: 없음

**예상 결과**:
- `재무 데이터 검토`(FIN팀) → `결과 종합 및 마감`(CO팀) 방향 화살표 존재
- `물류 데이터 검토`(LOG팀) → `결과 종합 및 마감`(CO팀) 방향 화살표 존재
- 두 화살표 모두 레인 전환이므로 **회색 점선**, 꺾인 경로로 CO팀 레인에 합류

**확인 방법**: Step4 노드 왼쪽에 두 방향에서 화살표가 수렴해 들어오는 형태인지 시각 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-004 [정상] 👤 수동 — 기존 선형 프로세스 하위 호환

**사전조건**: 앱 실행 중

**실행 단계**:
1. 사이드바에서 `CO팀 > 마감 프로세스 > 물류마감 Check 프로세스` 선택
2. 프로세스 카드 ▼ 펼쳐서 SwimLane 확인
3. `가공비 마감`, `재료비 마감` 프로세스도 동일하게 확인

**입력값**: 없음 (기존 SAMPLE_DATA 사용)

**예상 결과**:
- `colIndex` 필드가 없는 기존 스텝들이 **순서대로 순차 배치**됨 (마지막 열 자동 배정 로직 적용)
- 같은 레인 연속 스텝은 **부서 색 실선** 화살표로 연결
- 레인 전환 스텝은 **회색 점선** 화살표로 연결
- 이전 Phase에서 동작하던 것과 시각적으로 동일

**확인 방법**: 3개 스텝이 각각 열1, 열2, 열3에 정상 배치되며 화살표가 순차 연결되는지 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-005 [엣지] 🤖 AI검증 — colIndex 없는 스텝 마지막 열 자동 배정

**사전조건**: SwimLane.jsx 소스 코드

**실행 단계**:
1. `SwimLane.jsx` 내 `stepsWithCol` 계산 코드 확인
2. `colIndex` 없을 때 "최대 colIndex + 1" 로직 확인

**입력값**: 없음

**예상 결과**:
```js
// reduce 기반 자동 배정 — 마지막 열 + 1
const stepsWithCol = safeSteps.reduce((acc, step) => {
  if (step.colIndex !== undefined && step.colIndex !== null && step.colIndex !== '') {
    acc.push({ ...step, colIndex: Number(step.colIndex) })
  } else {
    const maxCol = acc.length > 0 ? Math.max(...acc.map(s => s.colIndex)) : -1
    acc.push({ ...step, colIndex: maxCol + 1 })
  }
  return acc
}, [])
```
- colIndex 없으면 `Math.max(...) + 1` 로 마지막 열 뒤에 자동 배치

**확인 방법**: `SwimLane.jsx`에서 `maxCol + 1` 패턴 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-006 [엣지] 🤖 AI검증 — connections 자동 파생 (colIndex 그룹핑)

**사전조건**: SwimLane.jsx 소스 코드

**실행 단계**:
1. `SwimLane.jsx` 내 `resolvedConnections` 계산 코드 확인
2. colIndex 그룹핑 → 인접 열 전체 조합 연결 로직 확인

**입력값**: 없음

**예상 결과**:
```js
// colIndex별 그룹핑 후 인접 열 전체 조합 연결
const colGroups = {}
stepsWithCol.forEach(s => {
  if (!colGroups[s.colIndex]) colGroups[s.colIndex] = []
  colGroups[s.colIndex].push(s.id)
})
const resolvedConnections = []
for (let i = 0; i < colIndices.length - 1; i++) {
  const fromIds = colGroups[colIndices[i]]
  const toIds   = colGroups[colIndices[i + 1]]
  fromIds.forEach(fromId => toIds.forEach(toId => {
    resolvedConnections.push({ from: fromId, to: toId })
  }))
}
```
- `connections` prop 없음 — SwimLane이 colIndex만 보고 자동 계산
- 앞 열 스텝 × 뒷 열 스텝 = 모든 조합으로 분기·합류 자동 생성

**확인 방법**: `SwimLane.jsx`에서 `colGroups`, `resolvedConnections`, `fromIds.forEach(fromId => toIds.forEach(toId` 패턴 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-007 [엣지] 👤 수동 — 같은 colIndex 스텝이 여러 레인에 정상 배치

**사전조건**: 앱 실행 중, `부서 병렬 처리 프로세스` 카드 ▼ 펼침

**실행 단계**:
1. SwimLane 다이어그램에서 열1(2번째 열) 확인
2. FIN팀 레인 열1에 `재무 데이터 검토` 박스 확인
3. LOG팀 레인 열1에 `물류 데이터 검토` 박스 확인
4. CO팀 레인 열1이 빈 셀인지 확인

**입력값**: 없음

**예상 결과**:
- FIN팀과 LOG팀이 동일 colIndex(1)를 가지므로 열1에 **각각 하나씩** 박스 표시
- CO팀 레인의 열1은 스텝이 없으므로 **빈 셀** (박스 없음)
- FIN팀 박스 번호 뱃지 = ②, LOG팀 박스 번호 뱃지 = ③

**확인 방법**: 열1에 FIN팀(2번) + LOG팀(3번) 박스 2개가 세로로 나란히 배치되고 CO팀 셀은 비어있는지 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-008 [실패] 🤖 AI검증 — 존재하지 않는 stepId의 DOM ref null 가드

**사전조건**: SwimLane.jsx 소스 코드

**실행 단계**:
1. `useLayoutEffect` 내 화살표 경로 계산 코드 확인
2. `fromEl` 또는 `toEl`이 null일 때 처리 로직 확인

**입력값**: 없음

**예상 결과**:
```js
const fromEl = nodeRefs.current[from]
const toEl   = nodeRefs.current[to]
if (!fromEl || !toEl) return null   // null 가드 — 크래시 없이 스킵
```
- DOM에 없는 노드는 null 반환 → `.filter(Boolean)`으로 제거
- 앱이 크래시(흰 화면)되지 않음

**확인 방법**: `SwimLane.jsx`에서 `if (!fromEl || !toEl) return null` 패턴 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-009 [실패] 🤖 AI검증 — 빈 steps 배열 처리 (hooks 순서 규칙 준수)

**사전조건**: SwimLane.jsx 소스 코드

**실행 단계**:
1. `SwimLane.jsx` hooks 선언 위치 확인
2. early return이 hooks 이후에 배치됐는지 확인

**입력값**: `steps = []` 또는 `steps = undefined`

**예상 결과**:
- `useRef`, `useState`, `useLayoutEffect` 모두 함수 최상단(또는 early return 이전)에 선언
- `if (safeSteps.length === 0)` early return이 hooks 이후에 위치
- "단계가 없습니다" 텍스트가 React 렌더링 오류 없이 표시

**확인 방법**: hooks 선언부와 early return 위치 순서 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-010 [실패] 👤 수동 — 단일 스텝만 존재 시 화살표 없이 렌더링

**사전조건**: 앱 실행 중, 임시로 단일 스텝 프로세스 생성 가능한 상태

**실행 단계**:
1. `+ 프로세스 추가`로 새 프로세스 생성
2. 스텝 1개만 추가 (`CO팀`, 단계명: `단일 테스트`)
3. 해당 프로세스 카드 ▼ 펼쳐서 SwimLane 확인

**입력값**: 스텝 1개 (dept: "CO팀", title: "단일 테스트")

**예상 결과**:
- 스텝 박스 1개만 정상 렌더링
- 화살표 없음 (colIndices가 1개이므로 `resolvedConnections = []`)
- 앱 크래시(흰 화면) 없음, 콘솔 에러 없음

**확인 방법**: 브라우저 DevTools 콘솔에 React 에러 없는지 확인 + 스텝 박스 1개만 표시되는지 시각 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-011 [정상] 👤 수동 — StepModal Step 필드에서 colIndex 저장

**사전조건**: 앱 실행 중, 아무 프로세스의 LV3 화면 진입

**실행 단계**:
1. `+ 단계 추가` 클릭
2. 단계명: "테스트A", 담당: "CO팀", Step: `1` 입력 후 추가
3. `+ 단계 추가` 다시 클릭
4. 단계명: "테스트B", 담당: "FIN팀", Step: `1` 입력 후 추가
5. `+ 단계 추가` 다시 클릭
6. 단계명: "테스트C", 담당: "CO팀", Step: 비워두고 추가
7. LV2로 돌아가서 해당 프로세스 ▼ 펼침

**입력값**: 3개 스텝 — Step `1`, `1`, 미입력(자동)

**예상 결과**:
- SwimLane: 테스트A(CO팀)와 테스트B(FIN팀)가 **같은 열**에 표시 (둘 다 colIndex:1)
- 테스트C는 **다음 열**에 표시 (자동 배정 = 1+1=2)
- 수정(✏️)으로 테스트A 열면 Step 필드에 `1`이 채워져 있음
- 수정(✏️)으로 테스트C 열면 Step 필드가 **비어있음**

**확인 방법**: SwimLane 열 배치 + StepModal 재진입 시 Step 값 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-012 [정상] 👤 수동 — LV3 단계 목록이 colIndex 기준 정렬

**사전조건**: 앱 실행 중, 병렬 흐름 테스트 프로세스 LV3 진입

**실행 단계**:
1. `부서 병렬 처리 프로세스` 상세 보기 클릭 (LV3)
2. "전체 흐름도" 섹션의 LinearFlow 순서 확인
3. "단계별 상세" 섹션의 StepCard 목록 순서 확인

**입력값**: 없음 (더미 데이터: colIndex 0, 1, 1, 2)

**예상 결과**:
- **LinearFlow**: colIndex 순으로 정렬된 4개 노드 표시 (미결현황 → 재무검토 → 물류검토 → 결과종합)
- **StepCard 목록**: 동일하게 colIndex 순, 같은 colIndex는 배열 순서 유지
- 번호 뱃지: ①미결현황 ②재무검토 ③물류검토 ④결과종합

**확인 방법**: LV3 화면에서 StepCard 순서가 colIndex 오름차순인지 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-013 [엣지] 🤖 AI검증 — 같은 부서+같은 colIndex 스텝이 셀 안에 모두 표시

**사전조건**: SwimLane.jsx 소스 코드

**실행 단계**:
1. SwimLane.jsx 셀 렌더링 코드 확인
2. `.find()` 대신 `.filter()` 사용 여부 확인
3. 셀 내부에 여러 박스를 렌더링하는 `cellSteps.map()` 패턴 확인

**입력값**: 없음

**예상 결과**:
```js
// filter로 같은 셀에 여러 스텝 허용
const cellSteps = stepsWithCol.filter(
  s => (s.dept || '미지정') === dept && s.colIndex === colIdx
)
// 셀 내부에 여러 박스 렌더링
{cellSteps.map(step => (
  <div key={step.id} ref={el => { nodeRefs.current[step.id] = el }} ...>
```
- `.find()` 아닌 `.filter()` 사용 → 같은 (dept, colIndex)에 스텝 2개 이상 시 모두 표시
- 셀에 `gap: 6, flexWrap: 'wrap'` 스타일로 여러 박스 나란히 배치

**확인 방법**: `SwimLane.jsx`에서 `cellSteps = stepsWithCol.filter(`, `cellSteps.map(step` 패턴 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-014 [정상] 🤖 AI검증 — 번호 뱃지가 colIndex 정렬 순서 반영

**사전조건**: SwimLane.jsx, LV3View.jsx 소스 코드

**실행 단계**:
1. SwimLane.jsx에서 `stepNumberMap` 계산 코드 확인
2. LV3View.jsx에서 `sortedSteps` 정렬 코드 확인

**입력값**: 없음

**예상 결과**:

SwimLane.jsx:
```js
// colIndex 정렬 기준 순서 번호
const sortedForNumber = [...stepsWithCol].sort((a, b) => a.colIndex - b.colIndex)
const stepNumberMap = {}
sortedForNumber.forEach((s, i) => { stepNumberMap[s.id] = i + 1 })
```

LV3View.jsx:
```js
const sortedSteps = [...(proc?.steps ?? [])].map((s, i) => ({ ...s, _origIdx: i }))
sortedSteps.sort((a, b) => {
  const aCol = a.colIndex ?? ...
  const bCol = b.colIndex ?? ...
  if (aCol !== bCol) return aCol - bCol
  return a._origIdx - b._origIdx
})
```
- SwimLane: colIndex 정렬 순서대로 1, 2, 3, 4 번호 부여
- LV3: colIndex 정렬 후 `index + 1`로 StepCard 번호 표시
- 두 뷰의 번호가 일치

**확인 방법**: `sortedForNumber`, `sortedSteps.sort` 패턴 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

## 테스트 완료 체크리스트

- [ ] 모든 정상 케이스 Pass (TC-001 ~ TC-004, TC-011, TC-012, TC-014)
- [ ] 엣지 케이스에서 앱이 크래시 없이 처리됨 (TC-005 ~ TC-007, TC-013)
- [ ] 실패 케이스에서 null 가드·안내 문구가 정상 동작함 (TC-008 ~ TC-010)
- [ ] 기존 선형 프로세스(Phase 1~7 데이터)가 변경 없이 동일하게 렌더링됨
- [ ] StepModal에서 Step 필드 입력/미입력 모두 정상 동작
- [ ] LV3 단계 순서가 colIndex 기준 정렬됨

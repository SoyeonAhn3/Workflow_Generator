# 테스트 시나리오 — Phase 6 AI 자동 구조화

- **생성일**: 2026-03-16
- **대상 Phase**: Phase 6
- **구현 기능**: AddMethodModal (방법 선택), AIGenerateModal (3단계 위자드: 입력→로딩→미리보기), netlify/functions/claude.js (Claude API 프록시 + JSON 재시도 3회), LV3 자동 이동
- **전제조건**: `netlify dev` 실행 중 (npm run dev 아님). `.env.local`에 `ANTHROPIC_API_KEY` 설정됨. SAMPLE_DATA 상태 (CO팀 > 마감 프로세스 선택)

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | LV2에서 [+ 프로세스 추가] 클릭 → AddMethodModal 표시 | ✅ Pass | High |
| TC-002 | 정상 | AddMethodModal에서 직접 입력 선택 → 기존 AddModal 열림 | ✅ Pass | High |
| TC-003 | 정상 | AddMethodModal에서 AI 자동 생성 선택 → AIGenerateModal 열림 | ✅ Pass | High |
| TC-004 | 정상 | AIGenerateModal Step1 입력 → AI로 구조화 → Step2 로딩 → Step3 미리보기 | ✅ Pass | High |
| TC-005 | 정상 | Step3 [편집기에서 열기] → LV3 이동 + 생성된 단계 표시 | ✅ Pass | High |
| TC-006 | 정상 | Step3 [← 다시 입력] → Step1 복귀 + 입력값 유지 | ✅ Pass | Medium |
| TC-007 | 엣지 | 프로세스명 미입력 상태에서 [AI로 구조화] 클릭 → 경고 | ✅ Pass | High |
| TC-008 | 엣지 | 업무 흐름 설명 미입력 상태에서 [AI로 구조화] 클릭 → 경고 | ✅ Pass | High |
| TC-009 | 엣지 | 담당 부서 빈값 → deptName 폴백 적용 확인 | 🤖 AI검증 ✅ | Medium |
| TC-010 | 엣지 | AddMethodModal 오버레이 클릭 → 모달 닫힘 | ✅ Pass | Medium |
| TC-011 | 실패 | npm run dev 상태에서 AI 생성 시도 → 에러 메시지 + Step1 복귀 | ⏭ Skip | High |
| TC-012 | 실패 | API 호출 실패 (네트워크 단절) → 에러 메시지 + Step1 복귀 | ⏭ Skip | High |
| TC-013 | 실패 | JSON 재시도 3회 모두 실패 → 500 에러 반환 로직 | 🤖 AI검증 ✅ | High |
| TC-014 | 실패 | ANTHROPIC_API_KEY 미설정 시 → 에러 메시지 반환 | 🤖 AI검증 ✅ | High |

---

### TC-001 [정상] 👤 수동 — LV2에서 [+ 프로세스 추가] 클릭 → AddMethodModal 표시

**사전조건**: `netlify dev` 실행 중. CO팀 > 마감 프로세스 선택하여 LV2 화면 표시 상태
**실행 단계**:
1. LV2 하단 `+ 프로세스 추가` 점선 버튼 클릭

**입력값**: 없음
**예상 결과**: 화면 중앙에 AddMethodModal 팝업 표시. "프로세스 추가" 타이틀, "📝 직접 입력" 카드와 "✨ AI 자동 생성" 카드가 가로로 나란히 표시. 뒷배경은 어두운 반투명 오버레이
**확인 방법**: 모달에 두 카드가 모두 표시되는지 육안 확인. 기존 AddModal이 아닌 방법 선택 모달인지 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: AddMethodModal 정상 표시, 두 카드 확인

---

### TC-002 [정상] 👤 수동 — AddMethodModal에서 직접 입력 선택 → 기존 AddModal 열림

**사전조건**: TC-001 완료 (AddMethodModal 열린 상태)
**실행 단계**:
1. "📝 직접 입력" 카드 클릭

**입력값**: 없음
**예상 결과**: AddMethodModal 닫히고 기존 AddModal(프로세스명/부서/담당자/모듈/설명 입력 폼)이 표시됨. 기존 수동 입력 흐름과 동일하게 동작
**확인 방법**: AddModal 타이틀 및 입력 폼 필드 표시 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 직접 입력 선택 시 기존 AddModal 정상 열림

---

### TC-003 [정상] 👤 수동 — AddMethodModal에서 AI 자동 생성 선택 → AIGenerateModal 열림

**사전조건**: TC-001 완료 (AddMethodModal 열린 상태)
**실행 단계**:
1. "✨ AI 자동 생성" 카드 클릭

**입력값**: 없음
**예상 결과**: AddMethodModal 닫히고 AIGenerateModal이 표시됨. "✨ AI 자동 생성" 타이틀, 프로세스명/담당 부서/담당자 입력 필드와 업무 흐름 설명 textarea 표시. 담당 부서 기본값에 현재 선택된 부서명(CO팀) 자동 입력
**확인 방법**: AIGenerateModal 표시 확인. 담당 부서 필드에 "CO팀" 사전 입력 여부 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: AIGenerateModal 표시, 담당 부서 "CO팀" 사전 입력 확인

---

### TC-004 [정상] 👤 수동 — AIGenerateModal 정상 흐름: 입력 → 로딩 → 미리보기

**사전조건**: TC-003 완료 (AIGenerateModal Step1 표시 상태)
**실행 단계**:
1. 프로세스명: "테스트 프로세스" 입력
2. 업무 흐름 설명: "MB52에서 미결 현황 조회 → 담당자 메일 발송 → 처리 완료 후 재확인" 입력
3. [✨ AI로 구조화] 버튼 클릭
4. Step2 로딩 화면 확인
5. Step3 미리보기 화면 자동 전환 확인

**입력값**: 프로세스명: "테스트 프로세스", 설명: "MB52에서 미결 현황 조회 → 담당자 메일 발송 → 처리 완료 후 재확인"
**예상 결과**: 버튼 클릭 즉시 Step2로 전환되며 스피너 애니메이션 + "AI가 프로세스를 분석 중입니다..." 메시지 표시. Claude API 응답 후 Step3로 자동 전환. Step3에서 생성된 단계 카드 목록(3개 내외) 표시. 각 카드에 단계명/화면명/부서/PT 정보 표시
**확인 방법**: Step2 → Step3 자동 전환 확인. 생성된 단계가 입력한 업무 흐름 내용과 의미적으로 일치하는지 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: Step2→Step3 자동 전환 확인. ByteString 오류 수정 후 정상 동작 (직접 fetch() 사용으로 수정)

---

### TC-005 [정상] 👤 수동 — Step3 [편집기에서 열기] → LV3 이동 + 생성된 단계 표시

**사전조건**: TC-004 완료 (Step3 미리보기 표시 상태)
**실행 단계**:
1. [편집기에서 열기 →] 버튼 클릭

**입력값**: 없음
**예상 결과**: AIGenerateModal 닫힘. LV3 화면으로 자동 이동. 프로세스명 "테스트 프로세스" 헤더 표시. AI가 생성한 단계들이 LV3 단계 목록에 표시됨. LV2로 돌아가면 "마감 프로세스" 그룹 내 "테스트 프로세스"가 목록에 추가됨
**확인 방법**: LV3 상단 프로세스명 확인. 단계 목록이 Step3 미리보기와 동일한지 확인. LocalStorage에 새 프로세스 저장 여부(상단 저장 뱃지 KB 증가) 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: LV3 이동 + 생성된 단계 표시 확인. TC-005 테스트 중 5가지 수정사항 도출 → 개발 반영

---

### TC-006 [정상] 👤 수동 — Step3 [← 다시 입력] → Step1 복귀

**사전조건**: TC-004 완료 (Step3 미리보기 표시 상태)
**실행 단계**:
1. [← 다시 입력] 버튼 클릭

**입력값**: 없음
**예상 결과**: Step3에서 Step1(입력 폼)으로 복귀. 이전에 입력한 프로세스명/담당 부서/업무 흐름 설명 값이 폼에 그대로 유지됨
**확인 방법**: Step1 입력 폼 표시 확인. 이전 입력값 유지 여부 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: Step1 복귀 + 이전 입력값 유지 확인

---

### TC-007 [엣지] 👤 수동 — 프로세스명 미입력 상태에서 [AI로 구조화] 클릭

**사전조건**: AIGenerateModal Step1 열린 상태 (TC-003 완료)
**실행 단계**:
1. 프로세스명 필드를 비워둔 채로
2. 업무 흐름 설명에 "MB52 조회 후 처리" 입력
3. [✨ AI로 구조화] 버튼 클릭

**입력값**: 프로세스명: "" (빈값), 설명: "MB52 조회 후 처리"
**예상 결과**: API 호출 없이 "프로세스명을 입력하세요" alert 표시. Step1 화면 유지됨
**확인 방법**: alert 메시지 확인. Step2 로딩 화면으로 전환되지 않는지 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: alert "프로세스명을 입력하세요" 정상 표시, Step2 전환 없음 확인

---

### TC-008 [엣지] 👤 수동 — 업무 흐름 설명 미입력 상태에서 [AI로 구조화] 클릭

**사전조건**: AIGenerateModal Step1 열린 상태
**실행 단계**:
1. 프로세스명: "테스트" 입력
2. 업무 흐름 설명 textarea는 비워둔 채로
3. [✨ AI로 구조화] 버튼 클릭

**입력값**: 프로세스명: "테스트", 설명: "" (빈값)
**예상 결과**: API 호출 없이 "업무 흐름 설명을 입력하세요" alert 표시. Step1 화면 유지됨
**확인 방법**: alert 메시지 확인. Step2로 전환 안 됨 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: alert "업무 흐름 설명을 입력하세요" 정상 표시, Step2 전환 없음 확인

---

### TC-009 [엣지] 🤖 AI검증 — 담당 부서 빈값 → deptName 폴백 적용

**사전조건**: 코드 분석
**실행 단계**:
1. AIGenerateModal.jsx handleGenerate 함수 코드 확인
2. netlify/functions/claude.js 요청 처리 확인

**입력값**: `dept` 필드 빈값 (`""`)
**예상 결과**: `AIGenerateModal.jsx` 내 `dept.trim() || deptName` 폴백으로 props의 deptName이 사용됨. API 요청 body의 `dept`에 빈값 대신 부모에서 전달된 deptName이 들어감. 생성된 단계의 `dept` 필드도 `s.dept || dept.trim() || deptName` 폴백으로 채워짐
**확인 방법**: AIGenerateModal.jsx:38 `dept.trim() || deptName` 및 :56 `s.dept || dept.trim() || deptName` 폴백 처리 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: AIGenerateModal.jsx:43,59,81 `dept.trim() || deptName` 폴백 3곳 정상 확인

---

### TC-010 [엣지] 👤 수동 — AddMethodModal 오버레이 클릭 → 모달 닫힘

**사전조건**: TC-001 완료 (AddMethodModal 열린 상태)
**실행 단계**:
1. 모달 바깥 영역 (반투명 오버레이) 클릭
2. 모달 내부 카드 클릭 후 닫히지 않는지 확인

**입력값**: 오버레이 영역 클릭
**예상 결과**: 오버레이 클릭 시 AddMethodModal 닫히고 LV2 화면으로 복귀. 카드 내부 클릭 시 닫히지 않고 해당 동작(직접 입력/AI 자동 생성) 실행
**확인 방법**: 오버레이 클릭 후 모달 사라짐 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 오버레이 클릭 시 모달 정상 닫힘 확인

---

### TC-011 [실패] 👤 수동 — npm run dev 상태에서 AI 생성 시도 → 에러 메시지

**사전조건**: `netlify dev` 대신 `npm run dev`로 앱 실행된 상태
**실행 단계**:
1. CO팀 > 마감 프로세스 선택 → LV2
2. [+ 프로세스 추가] → AddMethodModal → [✨ AI 자동 생성]
3. 프로세스명 + 설명 입력 후 [✨ AI로 구조화] 클릭

**입력값**: 프로세스명: "테스트", 설명: "조회 후 처리"
**예상 결과**: Step2 로딩 표시 후 `/.netlify/functions/claude` 404 오류 발생. AIGenerateModal Step1으로 자동 복귀. 붉은 에러 박스에 "AI 생성에 실패했습니다. 다시 시도해 주세요." 메시지 표시
**확인 방법**: Step1 복귀 확인. 에러 메시지 빨간 박스 표시 확인. Step3 미리보기로 진행되지 않음 확인

**결과**: [ ] Pass  [ ] Fail  [x] Skip  **코멘트**: 환경 제약으로 건너뜀 (netlify dev 환경 전환 불편)

---

### TC-012 [실패] 👤 수동 — API 호출 실패 → 에러 메시지 + Step1 복귀

**사전조건**: `netlify dev` 실행 중. AIGenerateModal Step1 입력 완료 상태
**실행 단계**:
1. 브라우저 개발자도구 > Network 탭 > 오프라인 모드 설정
2. [✨ AI로 구조화] 클릭
3. 네트워크 오류 발생 확인

**입력값**: 네트워크 오프라인 상태
**예상 결과**: Step2 로딩 후 네트워크 오류로 fetch 실패. Step1으로 자동 복귀. 붉은 에러 박스에 실패 메시지 표시. 앱이 크래시 없이 다시 시도할 수 있는 상태 유지
**확인 방법**: 오프라인 설정 → 시도 → Step1 복귀 + 에러 박스 표시 확인. 다시 온라인 전환 후 재시도 가능한지 확인

**결과**: [ ] Pass  [ ] Fail  [x] Skip  **코멘트**: 네트워크 단절 시뮬레이션 환경 제약으로 건너뜀

---

### TC-013 [실패] 🤖 AI검증 — JSON 재시도 3회 모두 실패 → 500 에러 반환 로직

**사전조건**: 코드 분석
**실행 단계**:
1. netlify/functions/claude.js 재시도 로직 코드 확인

**입력값**: Claude API가 3회 연속 유효하지 않은 JSON 반환 (시뮬레이션)
**예상 결과**: `for (let attempt = 1; attempt <= MAX_RETRIES; attempt++)` 루프가 3회 실행됨. 각 실패 시 `currentPrompt`를 재시도 프롬프트로 교체. 3회 모두 실패 시 `statusCode: 500`과 에러 메시지 반환. 프론트엔드 catch 블록에서 수신하여 Step1 복귀 처리
**확인 방법**: claude.js:53~76 for 루프 및 catch 블록 확인. MAX_RETRIES=3 상수 확인. 루프 탈출 후 500 반환 코드(82~87행) 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: claude.js:8 MAX_RETRIES=3, :51 for 루프, :78 재시도 프롬프트 교체, :88~92 500 반환 모두 확인

---

### TC-014 [실패] 🤖 AI검증 — ANTHROPIC_API_KEY 미설정 시 에러 반환

**사전조건**: 코드 분석
**실행 단계**:
1. netlify/functions/claude.js API 키 검증 코드 확인

**입력값**: `process.env.ANTHROPIC_API_KEY = undefined`
**예상 결과**: `if (!apiKey)` 조건에서 즉시 `statusCode: 500`, `{ error: "ANTHROPIC_API_KEY not configured" }` 반환. Claude API 호출 없이 조기 종료. 프론트엔드에서 에러를 수신하여 Step1 복귀 + 에러 메시지 표시
**확인 방법**: claude.js:13~18 apiKey 검증 블록 확인. 에러 메시지가 "ANTHROPIC_API_KEY not configured"인지 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: claude.js:13~18 `if (!apiKey)` 검증 블록 + 500 반환 확인

---

## 테스트 완료 체크리스트

- [x] AddMethodModal 표시 및 카드 선택 동작 (TC-001~003) ✅
- [x] AI 생성 전체 흐름 — 입력→로딩→미리보기→LV3 이동 (TC-004~005) ✅
- [x] 다시 입력 버튼으로 Step1 복귀 (TC-006) ✅
- [x] 필수 입력값 누락 경고 처리 (TC-007~008) ✅
- [x] 빈값 폴백 및 오버레이 닫힘 (TC-009~010) ✅
- [~] 실패 케이스에서 Step1 복귀 + 에러 메시지 표시 (TC-011~012) ⏭ Skip
- [x] 서버사이드 JSON 재시도 및 API 키 검증 로직 (TC-013~014) 🤖 AI검증 ✅

---

## 수정 이력 (TC-005 테스트 중 도출)

### 기능 추가 / 수정 (5건)

| # | 항목 | 내용 |
|---|------|------|
| M-001 | AI 생성 시 description 필드 제거 | AIGenerateModal에서 생성된 단계에 `description: ''` 고정 (사용자 불필요 판단) |
| M-002 | LV3 프로세스 수정 버튼 추가 | LV3View 헤더에 "✏ 수정" 버튼 추가 → EditProcModal 연결 |
| M-003 | LV2 프로세스 카드 연필 아이콘 추가 | 프로세스 카드 우측에 ✏ 버튼 추가 → EditProcModal 연결 |
| M-004 | LV1 그룹 카드 수정 버튼 + 부서 이동 기능 | 그룹 카드에 ✏ 버튼 추가 → EditGroupModal. 부서 변경 시 그룹 이동 처리 |
| M-005 | 그룹 추가 시 담당부서 필수 입력 | AddModal에서 그룹 레벨일 때 dept 필드 필수 검증 추가 |

### 버그 수정 (3건)

| # | 버그 | 수정 내용 |
|---|------|----------|
| B-001 | LV3 description 수정 후 LV1 왕복 시 미반영 | `handleEditProcSave`에 `setSelDept` 업데이트 추가 — selDept 전체 재동기화 |
| B-002 | EditGroupModal 불필요한 부서 안내 문구 | "현재 부서", "존재하는 부서 목록" 안내 문구 UI에서 제거 |
| B-003 | 수정 후 화면 이동 (LV2→LV3, LV1→LV2) | `prev?.id === updated.id ? updated : prev` 패턴으로 수정 — 현재 뷰에서 수정 시 화면 이동 방지 |
